{
  "installCommand": "git clone --depth 1 --branch stable https://github.com/flutter/flutter.git flutter-sdk && flutter-sdk/bin/flutter precache --web && cd unburden_app && ../flutter-sdk/bin/flutter pub get",
  "buildCommand": "cd unburden_app && ../flutter-sdk/bin/flutter build web --release --dart-define=API_BASE_URL=$API_BASE_URL",
  "outputDirectory": "unburden_app/build/web",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
